import { createClient } from '@supabase/supabase-js'; import fs from 'fs'; import path from 'path'; import * as dotenv from 'dotenv'; dotenv.config(); const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const htmlBuffer = fs.readFileSync('C:/Users/crist/.gemini/antigravity/scratch/ai-influencer/index.html'); const cssBuffer = fs.readFileSync('C:/Users/crist/.gemini/antigravity/scratch/ai-influencer/style.css'); const jsBuffer = fs.readFileSync('C:/Users/crist/.gemini/antigravity/scratch/ai-influencer/script.js'); await supabase.storage.from('media').upload('dashboard/index.html', htmlBuffer, { contentType: 'text/html', upsert: true }); await supabase.storage.from('media').upload('dashboard/style.css', cssBuffer, { contentType: 'text/css', upsert: true }); await supabase.storage.from('media').upload('dashboard/script.js', jsBuffer, { contentType: 'application/javascript', upsert: true }); console.log('DEPLOYED: ' + supabase.storage.from('media').getPublicUrl('dashboard/index.html').data.publicUrl); } run();
