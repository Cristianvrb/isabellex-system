import { createClient } from '@supabase/supabase-js'; import fs from 'fs'; import path from 'path'; import * as dotenv from 'dotenv'; dotenv.config(); const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const imgBuf = fs.readFileSync('drive_posts/isabellex_mirror_selfie.png'); const fileName = 'post_mirror_selfie_' + Date.now() + '.png'; const { error } = await supabase.storage.from('media').upload(fileName, imgBuf, { contentType: 'image/png', upsert: true }); if (error) { console.log('UPLOAD ERROR:', error.message); return; } const { data } = supabase.storage.from('media').getPublicUrl(fileName); const postData = { id: 'post_mirror_selfie', type: 'image', imagePath: path.resolve('drive_posts/isabellex_mirror_selfie.png'), urlMock: data.publicUrl, legenda: 'Tentando provar pro algoritmo que essa de fato sou eu. ????? #aiinfluencer #mirrorselfie ?', status: 'pendente_agendamento', scheduledFor: '2026-03-22T18:00:00.000Z', createdAt: new Date().toISOString() }; fs.writeFileSync('drive_posts/post_mirror_selfie.json', JSON.stringify(postData, null, 2)); console.log('POST CRIADO OK:', data.publicUrl); } run();
